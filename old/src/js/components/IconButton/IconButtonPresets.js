import './IconButtonPresets.less';

const Edit = {
  icon: 'pencil'
};

const Error = {
  icon: 'circle-warning',
  button: 'error-outline',
  noBorder: true
};

const Filter = {
  icon: 'filter'
};

const Flag = {
  icon: 'flag'
};

const Info = {
  icon: 'info',
  noBorder: true
};

const Next = {
  icon: 'next'
};

const Plus = {
  icon: 'plus'
};

const Prev = {
  icon: 'prev'
};

const Reset = {
  icon: 'reset'
};

const Undo = {
  icon: 'undo'
};

const Upload = {
  icon: 'upload'
};

const Xmark = {
  icon: 'xmark'
};

export default {
  Edit,
  Error,
  Filter,
  Flag,
  Info,
  Next,
  Plus,
  Prev,
  Reset,
  Undo,
  Upload,
  Xmark
};
